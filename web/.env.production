# 排行榜的两个后端地址。都不是密钥——访问者在浏览器网络面板里都能看到，
# 写入接口按 owner token 授权，读取的对象本来就是公开的。
# 环境 endfield-d3gdy9wg4afba9d16（ap-shanghai）。

# 榜单与记录的读取地址：云存储桶的公共域名。浏览器用 <script> 直接从 CDN 拉，
# 不经过云函数、网关和数据库。留空则隐藏排行榜入口。
VITE_COMBAT_STORAGE=https://656e-endfield-d3gdy9wg4afba9d16-1474357318.tcb.qcloud.la/combat

# 写入接口：网关路由 /combat -> combat-api。只在上传、删除、改视频号、
# 改上榜意愿时被调用。留空则只能看不能传。
VITE_CLOUDBASE_API=https://endfield-d3gdy9wg4afba9d16-1474357318.ap-shanghai.app.tcloudbase.com/combat
